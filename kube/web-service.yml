apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    name: web
spec:
  type: LoadBalancer
  loadBalancerIP: 104.197.148.99
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    name: web
